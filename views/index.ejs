<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kayubo | Node + Tailwind</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-800 min-h-screen">

    <div class="max-w-4xl mx-auto p-8">
        <header class="text-center mb-12">
            <h1 class="text-4xl font-extrabold text-indigo-600 mb-2">¡Bienvenido, <%= usuario %>!</h1>
            <p class="text-slate-500">Panel de control de noticias y contacto</p>
        </header>

        <div class="grid md:grid-cols-2 gap-8">
            <section>
                <h2 class="text-2xl font-bold mb-6 flex items-center">
                    <span class="mr-2">🗞️</span> Últimas Noticias
                </h2>
                <div class="space-y-4">
                    <% noticias.forEach(noticia => { %>
                        <div class="bg-white p-5 rounded-xl shadow-sm border border-slate-200 hover:shadow-md transition-shadow">
                            <h3 class="font-bold text-lg text-indigo-900"><%= noticia.titulo %></h3>
                            
                            <div class="flex justify-between items-center mt-3 text-xs text-slate-400">
                                <p>Por: <span class="font-medium text-slate-600"><%= noticia.autor %></span></p>
                                <p>📅 <%= noticia.fecha || 'Reciente' %></p>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>

            <section class="bg-indigo-600 p-8 rounded-2xl shadow-xl text-white">
                <h2 class="text-2xl font-bold mb-6">Contáctanos</h2>
                <form action="/contacto" method="POST" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium mb-1">Tu nombre</label>
                        <input type="text" name="nombre" placeholder="Escribe tu nombre..." 
                               class="w-full px-4 py-3 rounded-lg bg-indigo-500 text-white placeholder-indigo-200 border-none focus:ring-2 focus:ring-white outline-none">
                    </div>
                    <button type="submit" 
                            class="w-full bg-white text-indigo-600 font-bold py-3 rounded-lg hover:bg-indigo-50 transition-colors shadow-lg">
                        Enviar mensaje
                    </button>
                </form>
            </section>
        </div>
    </div>

</body>
</html>